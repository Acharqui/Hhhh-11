name: Android Build

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

# إضافة المتغير البيئي لإيقاف تحذيرات Node مؤقتاً
env:
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  build:
    runs-on: ubuntu-22.04  # استخدام Ubuntu 22.04 للتأكد من الاستقرار مع Buildozer

    steps:
      - uses: actions/checkout@v4

      # تثبيت الاعتماديات مع تقليل السجلات لتسريع الخطوة
      - name: Install dependencies
        run: |
          sudo apt-get update -qq  
          sudo apt-get install -y -qq \
            build-essential \
            git \
            ffmpeg \
            libsdl2-dev \
            libsdl2-image-dev \
            libsdl2-mixer-dev \
            libsdl2-ttf-dev \
            libportmidi-dev \
            libswscale-dev \
            libavformat-dev \
            libavcodec-dev \
            libunwind-dev \
            zlib1g-dev
          sudo apt-get install -y -qq \
            libsqlite3-dev \
            sqlite3 \
            bzip2 \
            libbz2-dev \
            openssl \
            libgdbm-dev \
            libgdbm-compat-dev \
            liblzma-dev \
            libreadline-dev \
            uuid-dev \
            libgstreamer1.0 \
            gstreamer1.0-plugins-base \
            gstreamer1.0-plugins-good
          sudo apt-get install -y -qq \
            zip \
            unzip \
            autoconf \
            libtool \
            pkg-config \
            libncurses5-dev \
            libncursesw5-dev \
            libtinfo6 \
            cmake \
            libffi-dev \
            libssl-dev \
            automake

      # إعداد Java 17 المطلوب لـ Gradle الحديث
      - name: Setup Java 17 required by Gradle
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      # إعداد إصدار أحدث ومستقر من Python (3.11) لتوافقية أفضل مع أدوات الأندرويد
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # تثبيت وترقية Buildozer و Cython لأحدث الإصدارات المتوافقة
      - name: Install pip dependencies
        run: |
          python -m pip install --upgrade pip
          pip install --upgrade buildozer cython virtualenv

      # بناء التطبيق مع تفعيل السجلات بالكامل لرؤية الخطأ الحقيقي إذا حدث
      - name: Build with Buildozer
        id: buildozer
        run: |
          yes | buildozer android debug

      # رفع الحزم الناتجة في حال نجاح البناء
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: package
          path: |
            bin/*.apk
            bin/*.aab
